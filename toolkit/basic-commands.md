# Useful IT Support Commands

## Windows
ipconfig /all  
ipconfig /flushdns  
ipconfig /renew  
tracert google.com  
sfc /scannow  
chkdsk /f  
Get-Service  

## Linux
df -h  
free -h  
top  
journalctl -xe  
systemctl status SERVICE  
ip a  
ping -c 4 8.8.8.8  

## Mac
sudo killall -HUP mDNSResponder  
diskutil list  
top  
sudo ifconfig en0 down/up
